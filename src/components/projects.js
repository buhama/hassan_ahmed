import { Fragment } from 'react';

const projects = () => {
  return (
    <Fragment>
      <section className='profile'>
        <h2>What's good</h2>
      </section>
    </Fragment>
  );
};

export default projects;
