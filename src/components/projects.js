import { Fragment } from 'react';

const projects = () => {
  return (
    <div>
      <section className='projects'>
        <h2>What's good</h2>
      </section>
    </div>
  );
};

export default projects;
